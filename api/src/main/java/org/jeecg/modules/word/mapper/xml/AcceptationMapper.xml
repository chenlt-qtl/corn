<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.word.mapper.AcceptationMapper">

    <select id="getByWord" resultType="java.lang.String">
       select group_concat(concat(pos,' ', acceptation) separator "\n") as acceptation
        from word_acceptation where word_id=#{wordId}
    </select>

</mapper>